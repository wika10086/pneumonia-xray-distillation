# 预测结果中的评判原因

现在预测结果会给出“模型为什么这样判断”的解释。解释分为三部分：

1. `pneumonia` 概率和判断阈值：如果肺炎概率高于阈值，就判为肺炎；低于阈值，就判为正常。
2. 置信度等级：根据概率离阈值的距离分为很低、较低、中等、较高。
3. Grad-CAM 热力图：用于显示模型主要关注图像的哪些区域。

重要限制：

- 这些解释只是算法输出依据，不是医学诊断依据。
- Grad-CAM 只能说明模型关注区域，不等同于真实病灶位置。
- 如果概率非常接近阈值，应该优先人工复核。

命令行生成热力图示例：

```powershell
.\.venv\Scripts\python.exe predict_image.py --image D:\picdata\test\pneumonia\test_00000.png --device cuda --save-heatmaps
```

热力图默认保存到：

```text
outputs\heatmaps
```

图形界面中，单张图片预测完成后会在左侧同时显示原图和 Grad-CAM 热力图。批量预测时不会为每张图片自动生成热力图；选中某张图片后点击“重新预测”，即可为该图片生成并显示热力图。
